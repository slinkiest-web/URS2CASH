/**
 * Paystack client utilities (stub).
 *
 * Server-only. Do not import from client components.
 * Paystack secret key (PAYSTACK_SECRET_KEY) is read only in this module.
 */
import "server-only";

// TODO: implement initialize, verify, resolve-account, and refund helpers.
export {};
